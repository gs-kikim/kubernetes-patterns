apiVersion: v1
kind: Service
metadata:
  name: random-generator
  labels:
    pattern: ConfigurationResource
spec:
  type: NodePort
  selector:
    app: random-generator
  ports:
  - port: 8080
    targetPort: 8080
    nodePort: 30081
    protocol: TCP
    name: http
